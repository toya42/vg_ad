url: https://<your-org>.github.io/<repo>
template:
  bootstrap: 5
navbar:
  structure:
    left: [reference, articles]
reference:
  - title: "Core Pipeline"
    contents:
      - fetch_day
      - preprocess_day
      - build_reference_days
      - fit_fpca
      - project_fpca_scores
      - reconstruct_from_fpca
      - compute_metrics
      - fit_thresholds
      - decide_today
