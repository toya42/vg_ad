{
  "name": "vg_ad",
  "image": "rocker/verse:4.4.1",
  "workspaceFolder": "/workspaces/vg_ad",
  "postCreateCommand": "R -q -e \"if (!requireNamespace('renv', quietly=TRUE)) install.packages('renv'); renv::restore(prompt=FALSE)\"",
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "RDebugger.r-debugger"
      ]
    }
  }
}
